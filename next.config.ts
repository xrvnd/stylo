// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // You can keep other Next.js specific configurations here if needed.
  // The experimental and webpack sections are no longer necessary for this issue.
};

export default nextConfig;